Groovy application to:
- Allow user to write custom query in the input field
- Parse the user query and convert it into MYSQL query using prefix algorithm
- Run SQL query, fetch the results and show the sampe data (50 rows) on the screen
- Allow user to download the full data in the form of .csv file.

